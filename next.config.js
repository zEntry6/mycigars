/** @type {import('next').NextConfig} */
const nextConfig = {
  // Server-side rendering required for admin dashboard (cookies, file operations)
  // Remove 'output: export' to enable dynamic routes
}

module.exports = nextConfig
